__pycache__/
*.egg-info/
.pytest_cache/
.ruff_cache/
.venv/
dist/

# Front: dependencias, salida del build y caché del compilador. `dist/` de arriba ya
# taparía `web/dist/`, pero escrito así se lee qué es lo que no entra al repo.
web/node_modules/
web/dist/
web/.angular/
